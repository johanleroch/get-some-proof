# Fixture assets

Stand-ins for the development fixtures only (`/visual-evidence/*`, `/screens`,
`/kit`), never for the product. `bumpr-logo.svg` is the logo of Bumpr, the
fictional project every authenticated screenshot is taken on: drop a different
file at the same path to change what those screenshots show.
