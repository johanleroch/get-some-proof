# Keep an application audit log

The application records immutable, organization-scoped audit events for invitations, membership and role changes, organization settings, and sensitive resource operations. This log complements rather than replaces the internal authorization-change audit maintained by convex-authz, because neither source alone describes both business actions and permission changes; only Owners and Admins may view the application log.
