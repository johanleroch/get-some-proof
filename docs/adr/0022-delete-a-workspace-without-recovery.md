# Delete a workspace without recovery

Workspace Deletion requires recent authentication, typed Brand-name confirmation, and a separate irreversible confirmation after an optional data and video download offer. It immediately removes the Public Wall and Embedded Wall, ends the subscription, and permanently deletes private records and Mux media without a recovery window; a failed or abandoned export never initiates deletion.
