# Use Next.js App Router

The starter uses Next.js App Router rather than TanStack Start or a Vite SPA. The official Convex Better Auth integration provides a direct same-origin auth proxy, server utilities, cookies, and SSR support for Next.js, while its broader familiarity makes a cloneable starter easier to adopt; this deliberately accepts a server runtime instead of static-only hosting.
